{
  "name": "roads-to-rome",
  "version": "0.0.0",
  "bin": {
    "hello-rome": "node -r esbuild-register ./src/index.ts"
  },
  "scripts": {
    "test": "node -r esbuild-register ./src/index.ts",
    "lint": "rome check ./src/*",
    "format": "rome format ./src/* --write"
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "esbuild-register": "^3.3.3",
    "rome": "^0.10.1-next"
  }
}
